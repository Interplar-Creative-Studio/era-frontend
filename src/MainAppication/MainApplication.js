import React from "react";
import {Header} from "./Header/Header";

export const MainApplication = (props) => {
    return(
        <div>
            <Header/>
            MainApplication
        </div>
    );
};
